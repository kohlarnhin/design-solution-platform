server:
  port: 9000
spring:
  application:
    name: koh-auth
  cloud:
    kubernetes:
      config:
        enabled: true
        name: koh-user-config
        namespace: koh-cloud
      discovery:
        namespaces: koh-cloud

