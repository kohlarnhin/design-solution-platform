server:
  port: 9001
spring:
  application:
    name: koh-user
  profiles:
    active: dev
  cloud:
    kubernetes:
      config:
        name: koh-user-config
        namespace: koh-cloud
        enabled: true

